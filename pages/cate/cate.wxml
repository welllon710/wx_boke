<!-- 头部组件 -->
<tabs list="{{cateList}}" bind:cate="cate" ></tabs>
<text>作者寄语：书读百遍其义自见</text>
<!-- 内容 -->
<view class="content">
  <block wx:for="{{contentList}}" wx:key="{{index}}">
    <content  list="{{item}}"></content>
  </block>
</view>